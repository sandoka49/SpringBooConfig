# DEV environment
app:
  message:This is the propertiy file for the ${spring.application.name} specific to DEV Enviroment

spring:
  datasource:
        url:jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
        username:sa
        password:sa
        driver-class-name=org.h2.Driver
